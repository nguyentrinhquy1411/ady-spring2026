Collect global weather data (OpenWeatherMap, WeatherStack).

Clean and normalize the data (remove missing values, standardize units).

Perform analysis using Pandas, Matplotlib, and Seaborn.

Visualize the results using charts and graphs.